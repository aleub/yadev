.row
  .span8.offset2
    figure
      p
        |yadev
      figcaption
        | — yet another developer blog

    section
      each post in posts
        article
          header
            h2 #{post.title}
            em (#{post.ts} in 
              a(href="/cat/#{post.category}") #{post.category}
              |)
          section
            !{post.body} 

          a(href="/#{post.title}")
            i.icon-share
