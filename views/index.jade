h1= title
p Welcome to #{title}

each post in posts
  article
    h2 #{post.title}
      button.remove-post.inline.btn.btn-danger(href="#", data-m-id="#{post._id}")
            i.icon-trash.icon-white
    p #{post.body}
